server:
  port: 1001

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Auth
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      show-sql: true

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: "1HiThisIs@SrikanthPoodariHere2WeAreCreating1JwtKeyfor#theUsers.3UserAuthentication"